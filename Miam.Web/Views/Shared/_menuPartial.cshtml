@using Externalization
@using Miam.Domain.Application
<ul class="nav navbar-nav">

    @if (Request.IsAuthenticated && User.IsInRole(Role.Admin))
    {
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" id="admin-menu">Admin <b class="caret"></b></a>
            <ul class="dropdown-menu">
                <li>@Html.ActionLink(UiText.Restaurant.UPDATE_RESTAURANTS, MVC.Restaurant.Index(), new { id = "manage-restaurant-menu-item" }) </li>
                <li>@Html.ActionLink(UiText.Restaurant.ADD_RESTAURANT, MVC.Restaurant.Create(), new {id="add-restaurant-menu-item"}) </li>
            </ul>
        </li>
    }

    @if (Request.IsAuthenticated && User.IsInRole(Role.Writer))
    {
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" id="writer-menu">Chroniqueur<b class="caret"></b></a>
            <ul class="dropdown-menu">
                <li>@Html.ActionLink(UiText.Review.ADD_REVIEW, MVC.Review.Create(), new {id="add-review-menu-item"}) </li>
            </ul>
        </li>
    }

</ul>
