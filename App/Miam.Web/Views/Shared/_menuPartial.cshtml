@using Miam.Domain.RoleName
<ul class="nav navbar-nav">
    
@if (Request.IsAuthenticated && User.IsInRole(RoleName.Admin))
{
    <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Admin <b class="caret"></b></a>
        <ul class="dropdown-menu">
            <li>@Html.ActionLink("Editer les restaurants", MVC.Admin.Index()) </li>
        </ul>
    </li>
}

@if (Request.IsAuthenticated && User.IsInRole(RoleName.RegistredUser))
{
    <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">User <b class="caret"></b></a>
        <ul class="dropdown-menu">
            <li>@Html.ActionLink("Ajouter un restaurant", MVC.Restaurant.Create()) </li>
        </ul>
    </li>
}

</ul>
